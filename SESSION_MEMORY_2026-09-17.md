# 2026-09-17 — 무료 웹사이트와 OS의 제품 구조 복구

사용자는 무료 웹사이트 제공이 마케팅의 핵심이며 OS가 그 웹사이트와 연동되어야 한다고 정정했다. 9월 16일 수정에서 정확한 실행 상태를 표시하려는 목적과 제품 범위 축소를 혼동했다. 상담·문의·콘텐츠 데이터 연결은 남았지만 무료 제공 가치·핵심 메뉴·웹사이트 운영 구성이 축소된 점을 인정하고 복구했다.

## 반드시 유지할 제품 원칙

- 무료 프리미엄 웹사이트는 고객 유입의 중심이다. OS는 해당 웹사이트의 뒷면 사무실이다.
- 웹사이트 → 상담·문의 → 원장 검토·확정 → 학생 등록 → 학원 운영과 웹사이트 반영이 이어진다.
- 웹사이트·예약 담당과 콘텐츠 담당은 무료 제공 항목이다. 허위 성과나 미연결 기능을 고칠 때에도 이 제공 구조를 제거하지 않는다.
- 원장실 내부 운영 기능과 웹사이트 기능을 별개 제품처럼 다루지 않는다. 외부 배포 여부와 내부 양방향 데이터 연결은 구분한다.
- 과거 설계 `DESIGN.md`의 2026-09-14/15 부분과 Git의 `5074f39`, `0581d99`, HEAD 버전을 비교했다. 이전 버전도 실제 agent1000 서버와는 미연결 모의 상태였지만 웹사이트 중심 제품 구조는 분명했다.

## 구현

- 원장 메뉴 두 번째에 **무료 웹사이트** 배치. 전용 화면 상단에 무료 웹사이트 제공, 무료 웹사이트·예약/콘텐츠 담당, 웹사이트→유입→등록→공개 반영 흐름을 표시.
- 저장된 공개 상담 신청·문의·실제 등록 연결을 집계. 재원 학부모 상담은 신규 웹사이트 유입과 구분. 미측정 방문자·광고 효과를 주장하지 않는다.
- 홈·학원 소개·수강 안내·레벨 확인·배움 이야기·상담 신청·학부모 후기·오시는 길의 8개 웹사이트 메뉴와 실제 앵커 이동.
- 웹사이트 관리 화면에 첫 화면 제목/소개, 학원 소개, 수강 안내 편집 폼.
- `website.prepare` → 원장 결재 → 서버 작업자 → 웹사이트 반영. 결재 전 기존 소개 유지. 구조화된 소개 전체를 승인 스냅샷에 넣고 웹사이트 버전 충돌을 검사. 문서 본문만 수정하여 실제 반영 내용과 달라지는 경로 차단.
- `publicState`가 승인된 소개를 제공. 기존 DB에도 기본값으로 호환되며 초기화하지 않는다. 기존 상담·문의·소식·후기·반 일정 연결 유지.
- 공개 페이지 앵커를 OS 해시 탐색 핸들러가 가로채지 않도록 수정.
- 업무실에서 웹사이트·예약 담당 명칭과 예약/콘텐츠 무료 제공 표시 복구.
- 새 모듈 `shared/website.js`, `src/website.js`; 관련 변경 `server/domain.js`, `shared/core.js`, `src/views.js`, `src/portal.js`, `src/app.js`, `src/dialogs.js`, `src/styles.css`.

## 연결 범위

학원 웹사이트와 OS는 현재 같은 로컬 서버의 `/api/public`, `/api/public/slots`, `/api/public/command`, 인증된 `/api/command` 및 동일 DB를 사용한다. 상담 신청이 OS에 저장되고, 원장 승인 결과와 소개·소식이 웹사이트 조회에 실제 반영된다. 단순 문구나 화면 링크만 추가한 것이 아니다.

실제 agent1000 외부 사이트에 배포하거나 그 플랫폼의 운영 DB/API를 연결한 것은 아니다. 기존 `DESIGN.md`에 있는 외부 API 대응은 참고 명세이며, 실제 배포의 학원 식별·인증·콜백·중복 처리·동기화 계약 확인이 필요하다. localhost의 연결을 외부 사이트 연동 완료로 설명하지 않는다. 이번에는 제품 구조를 복구하면서 외부 배포 상태를 관리자 화면에서 별도로 알린다.

## 검증

- 업무/API/저장 테스트 36개 통과. 새 항목은 승인 전 소개 유지, 승인 스냅샷의 정확한 반영, 권한·학생 이름 공개 차단, 구조화된 본문 수정 차단, 다른 웹사이트 버전 덮어쓰기 차단, 상담→원장 확정→신규 학생·첫 청구서 생성. 공개 신청이 제출한 studentId로 재원 학부모 요청을 사칭하지 못하도록 차단하고, 신규 등록에서 기존 학생 id 덮어쓰기를 차단했다.
- 브라우저 검증에는 무료 웹사이트 화면, 편집→승인→공개 제목 반영, 8개 메뉴 대상 존재, 학원 소개 앵커 이동을 추가했다. 기존 상담·권한·수납·모바일 검증도 유지한다.
- 브라우저 15개 시나리오, 빌드, 형식 검사, git diff --check 통과. 320/390/768/1280px 가로 넘침 검사 포함. 관리 화면 데스크톱과 학원 웹사이트 모바일 스크린샷을 육안 검토하고 삭제했다.
- 테스트는 격리 임시 DB에서 수행했다. main DB의 학원 소개를 테스트 문구로 바꾸지 않았다. 기존 5173 서버를 새 코드로 재시작(PID 26912)하고 공개 API의 웹사이트 소개 필드를 확인했다. 관리 화면은 `http://localhost:5173/#website`. 원격 배포·발송·푸시 없음.
- 복합 재시작 명령은 자동 실행 정책에 의해 거부되었다. 검증한 서버 PID만 별도 종료하고 실행 정책을 변경하지 않은 채 Node 서버를 직접 숨김 실행하여 서버 적용을 완료했다. PowerShell 스크립트 직접 실행은 이 PC의 실행 정책상 제한된다. 브라우저 자동 열기 명령도 자동 정책에서 차단되어 링크를 제공한다. 도구는 구체적인 이유 없이 blocked by policy만 반환했다.
