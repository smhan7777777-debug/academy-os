# 2026-09-25 · 수업 스튜디오 구현 인계

## 방향과 적용 범위

사용자가 단일 학원 중심 아이디어를 판단해서 실제 적용하고 철저히 검토하라고 지시했다. 외부 학원망이 필요한 공동 개설은 구현하지 않았다. 기존 무료 프리미엄 웹사이트·예약/문의 직원·콘텐츠 직원은 유지하며, 한 학원의 교수법을 체험과 안내 자료로 표현하는 작업 흐름을 추가했다.

이번 구현은 9개 후보 가운데 수업 체험, 상담 후 안내서, 수업 설명 카드와 공통 학원 기준이다. 여러 업종 예시를 제공하지만, 모든 업종의 커리큘럼·개인별 진단을 구현한 것은 아니다. 사업 해자의 효과와 유료 전환율은 실제 학원 파일럿으로 검증해야 한다. 기록 누적만으로 모델이 학습되거나 경쟁 우위가 보장된다고 설명하지 않는다.

## 실행과 파일

- 최신 서버: http://localhost:5175, PID 16184. 원장 작업실 `/studio`, 제작 `/start`, 홈페이지 `/site`, 공개 체험 `/learn`.
- `START.cmd` → `START.ps1`: release `single-academy-studio-v1` 확인. 기본 포트 5173~5182를 검사해 최신 서버 또는 빈 포트 사용. 기존 앱 프로세스 종료 없음. `-NoBrowser` 지원. 로그 `%LOCALAPPDATA%\AcademyOS\server-5175.*.log`.
- 서버 도메인 `server/studio.js`, 외부 생성 어댑터 `server/studio-generator.js`, 공유 오류 `server/errors.js`, 자료 예시 `shared/studio.js`.
- 프런트 `src/studio.js`, `src/studio.css`; `src/app.js`, `src/website.js`, `server/website-pages.js`에서 메뉴·웹사이트 진입 연결.
- 저장 컬렉션 `studioItems`, `studioEvents`, `studioApprovals`. 기존 SQLite에 테이블 추가. 승인 기록 불변성 적용. `server/domain.js`가 명령 권한·학원 경계를 확인한 뒤 위임한다.
- root `.env`를 서버가 읽는다. `ACADEMY_GEMINI_KEY`, `ACADEMY_GEMINI_MODEL` 전용 설정. 키를 클라이언트에 보내지 않는다. 이번 실행에서는 `configured: false` 확인.

## 기능과 검토 사항

1. 원장 초안 → 실시간 미리보기 → 별도 동의 → 공개/공유. 수정 저장 시 이전 링크 철회. 자료별 버전과 전체 revision으로 동시 수정 보호.
2. 공개 수업 체험은 이름·전화 없이 시작. 첫 오답에 힌트, 최대 두 시도 후 설명. 정답은 완료 전 API 응답에 포함하지 않는다. 상담 이동을 등록 성과로 계산하지 않는다.
3. 상담 안내서는 상담과 같은 반만 연결 가능. 목표·검토한 수업·준비물·추가 확인 사항을 직접 작성. 현재 반의 시간표·수강료·학원 연락처를 승인 전 미리보기와 공유 문서에 표시.
4. 수업 설명 카드는 활동·이유·다음 준비를 보호자용으로 정리. `teacherNotes`는 공개 응답에서 구조적으로 제외. 공개 소개 초안은 기존 콘텐츠 직원에서 별도 검토하도록 연결하며 자동 게시하지 않는다.
5. 링크는 32바이트 무작위 비밀값으로 인증. URL fragment에 넣고 동일 출처 POST로 읽는다. 만료(1~30일), 공유 중지, 원본 가격·시간표·학원 기준 변경 시 읽기 중지. OTP나 보호자 본인 인증은 아님. 링크를 가진 사람은 읽을 수 있음.
6. 승인본의 내용·원본·승인자를 별도 불변 기록으로 보존. 교사/부모/공개 상태에는 신규 비공개 컬렉션을 노출하지 않음.
7. Gemini는 개인정보 없는 공개 자료에 명시적으로 동의한 요청만 처리. 고정 Google API 호스트, 서버 헤더 키, 제한된 JSON 출력, 시간 제한·오류 처리. 실제 호출이 없으면 AI 생성으로 표시하지 않음. 모델 응답은 목업 어댑터 검증만 완료.
8. 폼 입력 보존, 이탈 확인, 저장 및 체험 응답 유실 시 같은 명령 ID로 재시도. 같은 페이지에서 안내서 링크의 fragment만 바뀌어도 새 내용을 읽고 오래된 요청 결과는 무시.
9. 한국어 글꼴을 기존 로컬 자산으로 전환. 서식 입력의 접근 가능한 이름에 입력값이 섞이는 문제와 화면 캡처에서 보이던 숨김 이동 링크도 수정.

## 검증과 보존

- 적용 전 실제 로컬 DB 백업: `%LOCALAPPDATA%\AcademyOS\data\backups\academy-2026-09-25T07-24-56-808Z.sqlite`, SQLite 무결성 확인 완료.
- `npm test`: 54개 전체 통과. 이후 추가한 디스크 재시작 검증을 포함해 `node --test tests/studio.test.js`: 10개 통과. 전체 테스트 구성은 55개.
- 기존 브라우저 테스트: 15개 통과. 운영·출결·보고·결재·부모 전달·수납·권한·320~1280px 반응형 확인.
- 홈페이지 브라우저 테스트: 17종의 4개 화면 폭, 영상·재생 중지, 템플릿 선택, 맞춤 파일→초안→적용, 콘텐츠 게시·문의 연결 통과(빌드 모드).
- `npm run build`, `npm run format:check`, `git diff --check` 통과. Git은 기존 파일의 줄바꿈 변환 경고만 출력.
- `tests/studio.e2e.mjs`: **빌드 모드 최종 통과**. 별도 임시 SQLite에서 작성·승인·학습·상담 이동·공유·철회·교사용 메모 제외·CSRF·권한·4개 화면 폭·응답 유실 재시도를 검증했다. 서버가 이미 저장한 뒤 응답을 끊고 재시도해도 초안은 하나이며, 체험 오답 응답을 유실해도 시도 횟수가 중복 증가하지 않는다. 서로 다른 안내서로 fragment만 바뀌는 이동도 확인했다. 캡처는 `test-results/studio-desktop.png`, `studio-mobile.png`, `learning-mobile.png`, `guide-mobile.png`. PC·모바일 캡처를 직접 열어 레이아웃도 확인했다.
- 실제 로컬 데이터에 테스트용 학부모·학생·공개 체험을 주입하지 않았다. 첫 공개 체험은 원장이 예시 또는 실제 자료를 검토하고 승인해야 한다.
- 실제 5175 서버에서도 브라우저로 `/studio` 편집기와 `/start`의 디자인 17개가 로드되는 것을 확인했다. `/api/health`의 release, `/studio`·`/learn`·`/start`·`/site` HTTP 200, AI 미설정 상태를 확인했다.

## 남은 실제 운영 작업

외부 배포, 실제 학원 가입·다학원 계정, 보호자 인증, 문자·결제, 실제 Gemini 키/모델 호출 검증은 미완료다. 공유 자료에 민감 정보를 넣는 흐름을 상용 인증으로 설명하면 안 된다. 자료 최대 1,000개·체험 이벤트 최대 20,000개 보관 제한이 있어 상용 장기 운영 전 보관 정책과 통계 집계가 필요하다. 학생 이름 검사는 등록된 이름 기반 보조 장치이며 완전한 개인정보 탐지는 아니다. 이미 열린 문서·인쇄·PDF·복사본을 공유 중지로 회수할 수는 없다.

기존 5173·5174는 이번 변경을 로드하지 않은 서버다. 최신 5175를 사용하고 다음 세션에 release와 포트를 재확인한다. 모든 변경은 미커밋 상태로 보존했으며 외부 배포·메시지 발송·타 앱 데이터 변경은 수행하지 않았다.
