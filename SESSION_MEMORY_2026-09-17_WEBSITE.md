# 2026-09-17 — 6개 디자인·영상·무료 직원 두 명의 로컬 구현

## 사용자 의도와 확인된 원본

인계 추가: 사용자 요청에 따라 `MEMORY.md`에 재개 순서, 로컬 주소, 실행·백업 위치, 미커밋 상태, 개발용 연결 정보 대기 상태를 저장했다. 다음 세션은 해당 파일부터 읽는다. 인계 시 5173 서버 PID 23412를 재확인했으며 재개 시 다시 확인해야 한다.

사용자는 무료 영상 웹사이트가 고객 유입의 핵심이며, 기본 템플릿 6개 중 하나를 선택하고 예약·문의 직원 및 콘텐츠 생성 직원 두 명이 무료로 포함된다고 반복 정정했다. 라이브 agent1000.kr와 이전 자료를 조사한 뒤 분석대로 진행하라고 승인했다. 기존 OS가 플랫폼 전체를 대체한다고 가정하지 않는다.

라이브 디자인 ID: EDU_ACADEMY_MULTI_DEUNGYONG / GAGYO / GWANMUN / HANBYEOL / ITDA / JEONGJIN. 원본은 인접 `pet care and academy/academy/agent1000-academy-lab/setup/templates-db/templates.json`의 preview_html, 영상은 해당 lab의 `public/assets/videos/academy-v1.mp4`, v2 및 포스터. 기존 무료 직원 코드 CORE-01·CORE-08을 유지한다. 과거 로컬 주석의 템플릿 2개·갤러리 폐쇄는 현재 라이브 상태가 아니다.

## 이번 변경

- `/start`: 6개 디자인 비교, 실제 로컬 렌더러로 만든 미리보기 이미지, 전체 미리보기, 학원 정보 및 영상 선택, 원장 권한으로 적용, 성공 후 웹사이트/원장실 이동.
- `/site`: 선택한 디자인·영상 렌더링. 기존 `/?portal=1`도 유지. 공개 예약·문의·콘텐츠·후기 등은 기존 서버와 동일 기록을 사용한다.
- `shared/templates.js`: 원본 ID, 디자인 메타데이터, 무료 직원 정의의 공통 기준.
- `src/template-hero.js`: 원본 CSS를 Shadow DOM 안에서 사용하고 여섯 히어로 구성을 학원 데이터로 렌더링한다. 미확인 실적·지점·가격·가짜 동작은 이식하지 않는다. 전체 원본 멀티페이지 HTML을 그대로 복사한 것은 아니다. 하위 운영 섹션은 공통 UI를 공유한다.
- 영상 로컬 자산 두 개, 포스터, 무음 재생, 재생/멈춤 버튼, reduced-motion 지원. 서버 빌드 모드에도 MP4 MIME과 byte-range 응답 추가.
- `website.configure`: 허용된 템플릿·영상만 저장, 원장 권한·학원 경계·명령 멱등성 적용. 기존 학생·예약·게시물 보존. 오래된 소개 변경안은 무효화해 덮어쓰기를 차단.
- 공개 응답에 로컬 site ID와 academy ID, 현재 디자인·영상, 무료 직원 두 명 제공. 이는 운영 플랫폼 shop_id를 확보했다는 뜻이 아니다.
- 예약·문의는 무료 직원 한 명의 두 내부 처리기로 묶음. 토글이 두 처리기를 함께 제어. 업무실의 별도 문의 직원 카드는 제외.
- 콘텐츠 생성 직원의 주제·자료 입력 폼, `content.generate`, 근거 자료 보존, 수정·승인·웹사이트 반영. 현재 실행은 명시적인 local-composer이며 외부 모델 생성이 아니다.
- `/site`와 `/start`의 이벤트가 OS 해시 탐색·미저장 입력 상태와 섞이지 않도록 분리. 디자인 미리보기에서는 신청 폼 제출 비활성화.
- `scripts/import-academy-designs.mjs`: 원본 스타일·영상 재현 가능한 복사. `public/designs/provenance.json`에 원본 HTML 해시 기록. 디자인 미리보기 PNG는 제품 자산이므로 유지; `test-results`는 임시 검증 결과.

## 검증

- 업무/API/저장 테스트 40개 통과. 여섯 ID 유지·학생/예약 보존, 권한, 잘못된 영상/템플릿 거부, 디자인 변경 시 대기 소개 보호, 두 기능의 직원 토글, 자료 기반 초안→수정 승인→공개 반영 검증 포함.
- 기존 브라우저 15개 시나리오 통과.
- 새 `npm.cmd run test:website`: 디자인 6개 각각 영상 로드/재생/멈춤 및 320·390·768·1440px 검사. 선택·영상·학원 정보 저장/새로고침, 콘텐츠 승인 전 비공개/승인 후 사이트 반영, reduced-motion, MP4 부분 응답 검사. 개발 모드와 실제 빌드 모드 모두 통과.
- 테스트는 모두 격리된 임시 SQLite 사용. 갤러리·가교·한별 스크린샷 육안 검토. 한별 하단 문구의 영상 버튼 가림을 수정했고 실제 클릭 검증 통과.
- 빌드 및 형식 검사 통과. 기존 메인 DB 백업 후 무결성 확인하고 5173 서버를 갱신. 현재 PID 23412, Node --dev. 공개 API health와 site:forest, 템플릿 및 두 직원 응답 확인. 기존 메인 DB의 선택값이나 학원 이름을 테스트 값으로 바꾸지 않았다.

## 아직 미완료 — 혼동 금지

- Agent1000 운영 계정/인증/DB/API와의 실제 통합, 새 고객의 다중 학원 생성·소유권 연결·도메인 배포는 구현하지 않았다. `/start`는 현재 로컬 학원 웹사이트의 구성 화면이다.
- CORE-01·CORE-08의 운영 플랫폼 LLM/n8n을 이번 세션에서 호출하거나 연결하지 않았다. 실제 AI 생성에는 인증된 개발 환경과 연결 계약이 필요하다. 사용자에게 개발용 주소·설정 파일 위치를 비동기 질문했다. 키/암호 값을 채팅에 요구하지 않았다.
- 운영 서버의 `/api/agents/sns-content/generate`는 사용자 인증 및 active shop을 서버에서 확인하고 image_urls 기반 입력을 사용한다. 로컬 topic/facts 요청을 근거 없이 그 엔드포인트로 보내면 안 된다. 엔진/콘텐츠 스키마와 사용자 범위를 먼저 맞춰야 한다.
- 원본 코드가 있다는 이유만으로 운영에서 정상 실행된다고 주장하지 않는다. 로컬 자료 정리를 실제 AI 생성으로 설명하지 않는다. 운영 서비스 호출/배포 없음.
