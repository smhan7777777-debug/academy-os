# Academy OS v4 — 구현·검증 기록

## 요청과 범위

사용자는 먼저 엄격한 감사를 요청했고, 이후 발견한 문제를 현실적으로 수정하고 디자인을 개선하여 localhost에서 볼 수 있도록 명시적으로 승인했다. 이번 변경은 academy-os에 한정된다. sermon/worship/pet-care 앱은 변경하지 않았다. 커밋·원격 푸시·배포·실제 외부 발송은 수행하지 않았다.

`AUDIT_2026-09-16.md`는 변경 전 커밋 `9bf48b1`의 감사 기록이다. 현재 구현은 아래와 `README.md`를 기준으로 판단한다. 모든 문제가 있는 실제 서비스 운영체계를 완성했다고 해석하면 안 된다. 이번 결과는 서버에 저장되고 역할별로 검증 가능한 로컬 운영 앱이며, 실제 계정 온보딩과 외부 공급자 연동은 남아 있다.

## 실행 상태

- 원장실 `http://localhost:5173`, 공개 페이지 `http://localhost:5173/?portal=1`.
- Node 서버는 loopback에서 실행한다. 이 작업의 마지막 확인 시 PID는 23756이었다. 다음 세션에서는 포트와 명령줄을 다시 확인한다.
- `START.cmd` → `START.ps1`: Node 버전·포트·의존성을 확인하고 서버를 숨김 실행한 뒤 브라우저를 연다. 기존 서버가 있으면 재사용한다. PowerShell의 localhost IPv6 재시도 지연 때문에 상태 검사 주소는 `127.0.0.1`을 사용한다.
- 기본 데이터 `%LOCALAPPDATA%/AcademyOS/data/academy.sqlite`. OneDrive 소스 밖에 저장한다. WAL/FULL 트랜잭션을 사용한다.
- 서버 로그 `%LOCALAPPDATA%/AcademyOS/server.stdout.log`, `server.stderr.log`.
- 기본 체험 계정 5개. `ACADEMY_DEMO=0`은 생성된 개별 암호를 요구하지만 사용자·보호자 연결 자체는 여전히 예시 고정 계정이다. 암호 파일 내용은 이 문서에 복사하지 않는다.
- 기존 `wonjangsil-v3`는 건드리지 않았다. 설정에서 원본 내보내기 가능. 자동 이관은 하지 않았다.

## 감사 항목별 조치

| 기존 문제 | 이번 구현 |
|---|---|
| 상담 이름의 HTML/스크립트 실행 | 사용자 입력을 공통 escaping으로 출력하고 CSP 적용. 실제 악성 형태 이름을 공개 신청한 후 관리자에서 문자로만 표시되는지 검사 |
| 미입력 학생의 자동 출석 | 미관찰은 null 유지. 관찰한 학생만 저장, 전체 출석은 별도 명시 버튼 |
| 결석 학생의 허구 학습 보고 | 결석에는 숙제·이해도·공유 메모를 배제하고 미관찰 문구 사용 |
| 검수와 원장 권한 우회 | 서버 세션에서 원장·담당 강사·연결 학생 범위 검사. 리포트·문제·진로 안내는 지정 강사 검수 필요 |
| 승인한 본문과 공개본 불일치 | 본문·수신자·채널·근거 버전·문항을 불변 승인 스냅샷과 SHA-256 해시로 저장. 로컬 작업자는 해당 스냅샷을 사용 |
| 의미 변경 후 승인 유지 | 모든 본문 수정은 새 검수·결재. 승인된 원본은 보존. 일정·예약·환불의 구조화된 본문은 직접 수정 대신 업무 화면에서 새 요청 |
| 여러 요일이 한꺼번에 바뀌는 시간표 | 요일별 시작·종료·교실과 적용일별 버전. 강사·교실·개인 일정·예약 충돌 검사. 결재 전에는 시간표 원본 유지 |
| 잘못된 예약 가능 시간과 경합 | 미래 30분 구간, 담당 강사·공유 상담실, 요청·확정 예약을 검사. 트랜잭션 안에서 다시 확인. 24시간 또는 시작 시각 만료 |
| 탭 간 수납 내역 덮어쓰기 | SQLite 트랜잭션, 학원 변경 버전 충돌 409, 요청 idempotency, 입금 식별번호 고유 제약. 수납은 불변 이벤트 |
| 저장 실패를 성공으로 표시 | 저장 성공 응답 이후만 완료 표시. SQL 실패 롤백. API 오류와 최신 버전 충돌 안내, 폼 입력 보존 |
| 업무 직원 스위치·설정 허위 효과 | 실제 구현된 규칙·서식 업무 8개로 정리. 중지 시 새로운 준비 작업을 실행하지 않음. 공개 강사·좌석 설정 반영 |
| 보류 시간이 적용되지 않음 | 서버가 보류 시각을 저장하고 해당 시각이 지나면 적절한 검수/결재 상태로 복귀 |
| 공개 답변·공유 도구가 실동작과 다름 | 문의별 불투명 확인번호, 승인 답변의 원 질문 반영, 복합 질문 담당자 확인. 실제 TXT/CSV/SVG 파일과 localhost 주소 제공 |
| 다른 학년 문제·다른 방문자 결과 연결 | 준비된 학년/과목만 표시. 예시 검사로 명시. 결과별 토큰, 만료, 명시적 상담 연결, 중복 연결 차단. 학생 문제도 승인된 본인 문항만 표시 |
| 학생 신호와 파생 문서의 불일치 | 날짜가 있는 실제 관찰·수납·의견으로 공통 계산. 수납·동의·수신 정보·수업 회차 변경 시 오래된 안내 차단/무효화 |
| 날짜·통계·AI 성과의 허구 | 한국 날짜와 실제 저장 이벤트 기준. 광고비 절감·예측 성과·가짜 AI 실행 수치 제거. 예시 데이터·규칙 업무·외부 미연결을 명시 |
| 근거 없는 법률·경영지원 | 확인한 사용자 입력만 서식에 정리. 환불은 명시된 제한 조건에 대한 계산·로컬 장부 조정과 공식 출처. 실제 전문가 연결·송금 주장을 제거 |

## 추가 완성도와 디자인

- 학생 등록·수정·반 이동·퇴원·수강 기간·동의·관심 활동·개인 일정. 상담 확정 후 학생 등록과 첫 청구서 연결.
- 신규 반, 요일별 시간표 변경안, 청구 생성, 부분 입금, 장부 확인서, 제한된 환불 조정.
- 학부모 월별 의견과 재원 학부모 익명 후기, 학생 문항 풀이와 서버 채점.
- 학부모·학생에게 승인·로컬 반영된 본인 기록만 제공. 강사 응답에서 다른 반과 수납 데이터 제외.
- 두 탭의 계정 변경을 새 세션과 화면에 반영. 공개 신청은 메뉴 이동/안전한 주기 갱신 시 반영.
- 숲색 사이드 메뉴, 크림·세이지 카드, 명확한 숫자·타이포그래피, 일관된 SVG 아이콘, 결재함을 첫 화면에 배치.
- 결재 목록에서 잔액·기한·남은 회차를 읽을 수 있는 요약. 학생 검색, 역할별 메뉴, 현재 상태와 실행 결과 안내.
- 320px 이상 반응형, 모바일 44px 주요 버튼, 작은 화면의 빈 보조 카드 생략, 숨은 메뉴의 키보드 접근 차단.
- 네이티브 dialog와 추가 Tab 순환, 제목·레이블·상태 안내, 다크 모드와 reduced-motion 지원.

## 파일 구조와 설계 한계

`src/`는 UI/API/공개 페이지, `shared/`는 순수 계산, `server/`는 HTTP·업무 규칙·SQLite·예시 데이터·백업으로 분리했다. 날짜·금액·권한·예약은 언어 모델에 맡기지 않는다. 일반적인 불변 이력과 고유 키는 DB에서 추가로 검사한다.

SQLite의 학원별 엔티티 테이블은 JSON payload 중심이다. 학생·청구 등 모든 관계에 대해 정규화된 외래키가 있는 구조는 아니다. 작은 로컬 앱을 대상으로 전체 학원 상태를 읽고 저장하며, 다학원 SaaS 규모의 조회·개별 엔티티 동시성·마이그레이션·부하 검증은 완료하지 않았다. 새 업무를 추가할 때 `server/domain.js`의 업무별 모듈 분리와 명시적인 스키마/형식 검사를 이어갈 수 있다.

예약 확정과 공개 반영은 이 컴퓨터의 앱에서만 일어난다. 작업자는 서버가 실행 중일 때 돌아가며, 서버 재시작 후 대기 작업을 다시 처리한다. 외부 공급자 전달 보장이나 외부 발송 재시도·대사 체계는 구현했다고 주장하지 않는다.

## 검증 결과

- `npm.cmd test`: **32개 통과**. 관찰·검수·권한·승인 본문·보류·수납 중복·SQL 실패 롤백·시간표 충돌·예약 경합·공개 설정·문의·문항·반환 장부·수신자 변경·회차 변경·이전 초안 호환 처리 포함.
- 초기 로컬 DB에 수신 정보/남은 회차 근거가 없는 초안이 있으면 원본을 보존하고 근거 변경 상태로 전환한다. 현재 기록으로 새 자동 초안을 준비하며, 근거 없는 과거 초안의 승인을 허용하지 않는다. 마지막 서버 재시작에도 이 처리를 적용했다.
- 실제 파일 DB를 닫고 다시 연 뒤 비교. SQLite 백업을 새 DB로 열어 원본 상태와 비교. 두 DB 연결의 오래된 버전 저장 차단 검사.
- 암호 모드 HTTP 서버를 별도 임시 DB로 실행하여 로그인 실패/성공, 세션 종료, CSRF 거부, 제출한 역할 값으로 권한을 바꿀 수 없음을 검사.
- `npm.cmd run test:browser`: **14개 시나리오 통과**. 개발 모드에 이어 최종 빌드 파일을 제공하는 서버에서도 동일 검증 통과 (`ACADEMY_E2E_BUILT=1`). Windows Microsoft Edge headless 사용.
- 브라우저 시나리오: 원장 10개 경로, 첫 화면 결재함, 대화상자 Tab 포커스, 강사 범위, 미관찰 유지, 실제 기록→검수→결재→학부모 본문, 수납 재접속, 수정 콘텐츠 공개, 악성 이름 문자 출력, 미인증/교차 Origin 거부, 탭 간 계정 전환, 320/390/768/1280px 가로 넘침, 공개 모바일, 다크 본문/주요 버튼 대비 4.5:1 이상.
- `npm.cmd run build`, `npm.cmd run format:check`, `git diff --check` 통과. 패키지 설치 시 audit 0건. 원격 CI는 파일만 추가했으며 아직 푸시·실행하지 않았다.
- main localhost에서 원장 10개 화면과 공개 페이지를 읽기 전용으로 재확인. 화면 JS 오류 없음. 데스크톱/모바일 스크린샷 육안 검토 후 삭제.
- `npm.cmd run backup`: 실제 로컬 DB 백업과 integrity_check 성공. 백업 경로는 데이터 폴더의 `backups/`. 기존 서버 재사용·브라우저 열기 실행 파일 확인.
- 기능 테스트는 전부 별도 임시 DB에서 수행. main DB에는 테스트용 입금·악성 이름을 넣지 않았다. 사용자의 기존 브라우저 기록을 지우지 않았다.

## 실제 운영을 위해 남은 일

1. 사용자 초대·교사 관리·검증된 보호자/자녀 연결과 다학원 온보딩. 지금은 5개 고정 예시 계정이다.
2. 사용 중인 실제 학생 명부와 데이터 정의를 바탕으로 검토·중복 확인이 가능한 이관. 다중 반 수강, 일반 수납 취소/정정·은행 대사, 완전한 학사 관리 범위는 추가 설계가 필요하다.
3. 공급자가 확인된 LLM·문자/알림톡·결제·agent1000 연동. 실 발송·공급자 오류·불확실한 결과 대사·비용과 출력 평가. 현재 키나 API 계약을 임의로 가정하지 않았다.
4. 영속 서버/DB와 HTTPS를 포함한 실제 배포. 이전 정적 Vercel 배포 방식은 그대로 사용할 수 없다. 운영 계정·외부 백업·보관/삭제 정책·모니터링·부하 검증.
5. 실제 문항 권리·학년별 문제은행과 평가 기준, 교사 검수. 9개 자체 예시 문항은 진단 검사가 아니다.
6. SQLite 정규화·업무별 모듈 분리, 자동 백업 스케줄과 복구 화면, 더 넓은 접근성·실기기·실제 원장/강사 사용성 검증.

이 항목들은 localhost에서 연결된 작업 흐름의 결함을 고친 것과 구분하여 설명해야 한다. 사용자에게 다음 단계의 외부 서비스가 이미 연결되었거나 실제 운영 검증이 끝났다고 말하지 않는다.
